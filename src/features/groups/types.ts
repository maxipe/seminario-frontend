/**
 * Re-exports of core domain types scoped to the groups feature.
 * Keeps the feature self-contained while staying in sync with the global type definitions.
 */

export type { Group, Supplier } from '../../types';
